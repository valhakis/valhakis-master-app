var spawn = require('child_process').spawn;

var options = { 'stdio': 'inherit' };

var webpack_spawn = spawn('webpack', ['--watch'], options);
var nodemon_spawn = spawn('nodemon', ['server'], options);
